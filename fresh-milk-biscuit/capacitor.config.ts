import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fresh-milk-biscuit',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
